# Система промптов Live Hints

## Обзор

Live Hints использует систему профилей для генерации контекстных ответов LLM. Каждый профиль определяет:

- Системный промпт (инструкции для LLM)
- Few-shot примеры (примеры диалогов)
- Максимальное количество токенов
- Температуру генерации

## Текущие режимы

### 1. 🎯 Собеседование (`job_interview_ru`)

**Назначение**: Помощь в ответах на технических собеседованиях

- **Длина ответа**: 200-300 слов (1-2 минуты)
- **Структура**: STAR (Situation-Task-Action-Result)
- **Особенности**: Фокус на технических вопросах и опыте

### 2. 💼 Бизнес-встреча (`business_meeting`)

**Назначение**: Формулирование аргументов для переговоров

- **Длина ответа**: 150-250 слов (1-1.5 минуты)
- **Структура**: Тезис → Аргументы → Решение
- **Особенности**: Деловой стиль, фокус на метриках

### 3. 📋 Дейлик/Созвон (`daily_sync`)

**Назначение**: Краткие статус-отчёты команде

- **Длина ответа**: 100-150 слов (30-45 секунд)
- **Структура**: Вчера → Сегодня → Проблемы
- **Особенности**: Лаконичность, эмодзи для визуализации

### 4. 🎤 Презентация (`presentation`)

**Назначение**: Убедительные питчи и демонстрации

- **Длина ответа**: 200-300 слов (2-3 минуты)
- **Структура**: Hook → Problem → Solution → Value
- **Особенности**: Увлекательный стиль, призыв к действию

### 5. ⚙️ Свой промпт (`custom`)

**Назначение**: Пользовательские инструкции

- **Длина ответа**: Настраивается
- **Структура**: Определяется пользователем
- **Особенности**: Полная свобода настройки

## Как это работает

1. **Выбор режима** в UI или онбординге
2. **Загрузка контекста** (резюме, вакансия, доп. информация)
3. **Генерация системного промпта** с подстановкой контекста
4. **Добавление few-shot примеров** для лучшего качества
5. **Отправка в LLM** для генерации ответа

## Добавление нового режима

1. Создайте словарь с промптом в `python/prompts.py`:

```python
_NEW_MODE = {
    'system': (
        'Ты AI-ассистент для...\n\n'
        '## Правила ответа:\n'
        '1. **Длина**: X слов\n'
        '...\n\n'
        '## Контекст участника:\n{user_context}\n\n'
    ),
    'few_shot_examples': [
        {
            'user': 'Пример вопроса',
            'assistant': 'Пример ответа'
        }
    ],
    'max_tokens': 800,
    'temperature': 0.7
}
```

1. Добавьте в `PROFILE_PROMPTS`:

```python
PROFILE_PROMPTS = {
    # ... существующие профили
    'new_mode': _NEW_MODE,
}
```

1. Обновите UI в `renderer/index.html`:

```html
<option value="new_mode">🔧 Новый режим</option>
```

1. При необходимости добавьте в онбординг (`renderer/onboarding.html`)

## Настройка контекста

Каждый режим может использовать дополнительный контекст:

- **Собеседование**: Резюме и вакансия
- **Бизнес-встреча**: Название проекта, роль, команда
- **Дейлик**: Текущие задачи, номер спринта
- **Презентация**: Описание продукта, целевая аудитория

Контекст сохраняется в файлах:

- `data/resume.txt` - резюме
- `data/vacancy.txt` - вакансия
- `data/user_context.txt` - дополнительный контекст

## Оптимизация промптов

- **Длина**: Оптимально 300-500 токенов для системного промпта
- **Чёткость**: Используйте конкретные инструкции и примеры
- **Контекст**: Вставляйте переменные через `{user_context}`
- **Few-shot**: 2-3 примера для улучшения качества

## Тестирование

Запустите тесты для проверки промптов:

```bash
cd python && ../venv/Scripts/python.exe -m pytest ../tests/unit/test_prompts_new.py -v
```
